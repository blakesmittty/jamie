#include <MCP.h>

// need to have a vector of clients and have them randomly accessable so we can have handles on each

/** 
 *  after building the client, we need to generate an mcp::tool for each one so we can provide the model with context.
 *  We track these in the tools vector. 
 *  
 */

MCP::MCP(struct Config *config) {
    
}